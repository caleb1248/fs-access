const app = (
  <div id="app">
    <div id="sidebar">
      <button
        onclick={() => {
          alert("hi");
        }}
      >
        hi
      </button>
    </div>
  </div>
);

export default app;
